<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.lius.mvc.mapper.UserMapper">
    <resultMap id="userResultMap" type="xyz.lius.mvc.entity.User">
    </resultMap>

    <select id="findAll" resultMap="userResultMap">
        select * from user
    </select>
</mapper>
