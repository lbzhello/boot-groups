version: "3"

services:

  zookeeper:
    image: liubaozhu/zookeeper
    #depends_on:
    # - ubuntu
    # build: .
    ports:
      - 2181:2181
    volumes:
      - /data/zookeeper:/tmp/zookeeper
    command: ["bash", "-c", "/app/zookeeper/start.sh"]